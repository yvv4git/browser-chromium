services:
  chromium:
    image: yvv4docker/browser-chromium:latest
    container_name: browser-chromium
    build: .
    ports:
      - "9222:9223"
      - "3000:3000"
    environment:
      - "RESOLUTION=${RESOLUTION:-1920x1080}"
      - "PROXY=${PROXY:-}"
      - "CHROME_ARGS_EXTRA=${CHROME_ARGS_EXTRA:-}"
    volumes:
      - "./profile:/config"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    shm_size: 2g
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
